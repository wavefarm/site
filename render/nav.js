var h = require('virtual-hyperscript')


module.exports = function (state) {
  if (state.ta) {
    // TODO Render ta nav
  }

  if (state.wgxc) {
    // TODO Render wgxc nav
  }

  if (state.mag) {
    // TODO Render mag nav
  }

  // No nav if not in subsite
  return ''
}
